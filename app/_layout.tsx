// app/_layout.tsx
/**
 * Layout principal de la aplicación.
 * Este archivo define la estructura de navegación global y envuelve todas las rutas.
 * Usa Stack de Expo Router para manejar la navegación entre pantallas.
 */
import { Stack } from 'expo-router';

export default function Layout() {
  return (
    <Stack>
      {/* Opciones globales de navegación pueden configurarse aquí */}
    </Stack>
  );
}