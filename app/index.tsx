// app/index.tsx
/**
 * Pantalla del Dashboard.
 * Es la pantalla principal que muestra un resumen del progreso, actividades recomendadas y próximos repasos.
 * Corresponde a la ruta raíz "/".
 */
import { View, Text } from 'react-native';

export default function Dashboard() {
  return (
    <View>
      <Text>Dashboard</Text>
    </View>
  );
}